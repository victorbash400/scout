# Minimal requirements for Scout Backend
# These are the only packages you need to install directly
# All other dependencies will be pulled in automatically

# AI Framework (pulls in boto3, opentelemetry, pydantic, etc.)
strands-agents==1.8.0
strands-agents-tools==0.2.7

# Web Framework (pulls in starlette, uvicorn, etc.)
fastapi==0.118.0

# PDF Processing
PyPDF2==3.0.1
reportlab==4.4.4
markdown==3.9

# Already included via strands-agents dependencies:
# - python-dotenv (via pydantic-settings)
# - httpx (via mcp)
# - python-multipart (via mcp)
# - uvicorn (via mcp)
# - beautifulsoup4 (via strands-agents-tools)
# - pydantic, boto3, etc.
